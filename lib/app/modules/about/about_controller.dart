import 'package:get/get.dart';

class AboutController extends GetxController {
  // Add any reactive variables or methods needed for the About page
  
  @override
  void onInit() {
    super.onInit();
    // Initialize any data or services needed for the About page
  }
  
  @override
  void onClose() {
    // Clean up any resources when the controller is closed
    super.onClose();
  }
}